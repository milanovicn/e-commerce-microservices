export const environment = {
  production: false,
  collectorUrl: 'http://localhost:4318',  // OTel Collector OTLP HTTP — exposed on host
  serviceName: 'ecommerce-frontend',      // change this per app
};